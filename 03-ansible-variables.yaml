- name: variables
  hosts: localhost
  become: yes
  vars: 
    USER: "Srinivasa"
    WISH: "Morning"
    NEWS: "selected for Devops Training"
    Fees: "RS: 15000"
  tasks:
    - name: Wishing
      ansible.builtin.debug:
        msg: "Hello {{ USER }}, Good {{ WISH }}"

    - name: Training
      hosts: all
      ansible.builtin.debug:
        msg: "Congrats!! you are {{ NEWS }} and the fees is {{ Fees }}"
