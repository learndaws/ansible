- name: variables
  hosts: localhost
  become: yes
  vars: 
    USER: "Srinivasa"
    WISH: "Morning"
    WISH-1: "Morning"
    
  tasks:
    - name: Wishing
      ansible.builtin.debug:
        msg: "Hello {{USER}}, Good {{WISH}}"
        msg: "Hello {{USER}}, Good {{WISH-1}}"
